// {{ module_name }}.cpp - AUTO-GENERATED by ReSource Recover
#include "{{ include_path }}"

{% for func in functions %}
// -------------------------------------------------------------------
// {{ func.display_name }}
// Address    : 0x{{ '%X' % func.address }}
// Raw name   : {{ func.name }}
// Source     : {{ func.source_hint or 'n/a' }}
// Class      : {{ func.class_hint or 'n/a' }}
// Module     : {{ func.module or 'misc' }} ({{ func.module_reason or 'n/a' }})
// Confidence : {{ func.confidence_level }} ({{ func.confidence_score }}/100)
// Reasons    : {{ func.confidence_reasons | join(', ') if func.confidence_reasons else 'n/a' }}
// Prototype  : {{ func.prototype or 'n/a' }}
extern "C" recovered_word_t {{ func.stub_name }}()
{
    // Buildable placeholder body. Original decompilation is preserved below.
{% if func.decompiled_code %}
{{ func.decompiled_code | comment_block }}
{% else %}
    // DECOMPILE FAILED: {{ func.decompile_error or 'unknown error' }}
{% endif %}
    return 0;
}

{% endfor %}
