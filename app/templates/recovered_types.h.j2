// recovered_types.h - AUTO-GENERATED by ReSource Recover
// Buildable compatibility header for reconstructed skeleton sources.
#pragma once

#include <cstddef>
#include <cstdint>

using recovered_word_t = std::uintptr_t;

// ===== Opaque type placeholders =====
{% for s in sanitized_structs %}
// Original struct {{ s.raw_name }} (size {{ s.size }} bytes)
struct {{ s.name }} {
    std::uint8_t opaque[{{ s.size }}];
};

{% endfor %}

// ===== Function stubs =====
#ifdef __cplusplus
extern "C" {
#endif

{% for art in artifacts %}
// 0x{{ '%X' % art.address }} {{ art.display_name }}
recovered_word_t {{ art.stub_name }}();
{% endfor %}

#ifdef __cplusplus
}
#endif
